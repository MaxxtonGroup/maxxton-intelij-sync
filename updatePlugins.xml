<?xml version="1.0" encoding="UTF-8"?>
<plugins>
  <!--
  Each <plugin> element (required) describes one plugin in the repository.
  Attributes:
   - "id" (required) - used by JetBrains IDEs to uniquely identify a plugin. Must match <id> in the plugin.xml file.
   - "url" (required) - URL to download the plugin JAR/ZIP file. Must be HTTPS.
   - "version" (required) - version of this plugin. Must match <version> in the plugin.xml file.
  -->
  <plugin
    id="com.maxxton.mold"
    url="https://github.com/MaxxtonGroup/maxxton-intelij-sync/releases/download/v1.3.0/plugin-1.3.0.jar"
    version="1.3.0">
    <name>
      Mold
    </name>

    <description>
      A simple settings synchronisation plugin for Maxxton developers.
    </description>

    <change-notes>
      First public release
    </change-notes>
    <!--
    The <idea-version> element (required) must match the same element in the plugin.xml file.
    -->
    <idea-version since-build="243" />
  </plugin>
</plugins>